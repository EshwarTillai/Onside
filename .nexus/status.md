# Status — Onside

**Last updated:** project kickoff.

## Where things stand

- Project scaffolded (Vite + React + TS, `strict: true` verified with a clean build).
- `/api` folder created (empty — proxy logic not written yet).
- `.env.example` in place.
- Specs, architecture, risks and backlog defined (see the other `.nexus/` files).

## Next step

- Get a football-data.org API key.
- Build the `/api` proxy route(s).
- Start on the Home view (list of the 5 competitions).
